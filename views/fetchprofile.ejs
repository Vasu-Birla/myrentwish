<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ciss Profile Scraper</title>
    <!-- Add your stylesheets or link to CDNs here -->
</head>
<body>
    <h1>Ciss Profile Scraper</h1>
    <form action="/fetch-profile" method="post">
        <label for="profileUrl">Enter OnlyFans Profile URL:</label>
        <input type="text" id="profileUrl" name="profile_url" required>
        <button type="submit">Fetch Profile Data</button>
    </form>

    <!-- Display profile data here -->
    <% if (profileData) { %>
        <div>
            <p><%= profileData %></p>
            <!-- <img src="<%= profileData.avatar %>" alt="Profile Avatar"> -->
            <!-- Add more profile data elements as needed -->
        </div>
    <% } %>

    <!-- Add your scripts or link to scripts at the end of the body if needed -->
</body>
</html>
